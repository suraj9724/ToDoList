import React from 'react';
import Notes from './Notes';

// Home component that renders the Notes component
const Home = () => {
    return (
        <div>
            <Notes />
        </div>
    );
};

export default Home;
